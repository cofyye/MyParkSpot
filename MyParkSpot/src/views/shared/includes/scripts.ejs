<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/socket.io/socket.io.js"></script>

<% if(messages.success) { %>
<script>
  Swal.fire({
    title: 'Success!',
    text: '<%= messages.success %>',
    icon: 'success',
    iconColor: '#000',
    customClass: {
      popup: 'rounded-3xl border border-gray-300 shadow-lg',
      title: 'font-serif text-2xl font-bold text-primary',
      content: 'text-gray-900',
      confirmButton:
        'rounded-full bg-primary px-4 py-2 font-medium text-white transition duration-300 ease-in-out hover:scale-105 hover:bg-hover',
    },
    buttonsStyling: false,
  });
</script>
<% } else if(messages.error) { %>
<script>
  Swal.fire({
    title: 'Error!',
    text: '<%= messages.error %>',
    icon: 'error',
    iconColor: '#000',
    customClass: {
      popup: 'rounded-3xl border border-gray-300 shadow-lg',
      title: 'font-serif text-2xl font-bold text-primary',
      content: 'text-gray-900',
      confirmButton:
        'rounded-full bg-primary px-4 py-2 font-medium text-white transition duration-300 ease-in-out hover:scale-105 hover:bg-hover',
    },
    buttonsStyling: false,
  });
</script>
<% } %>

<!-- Socket IO Client -->
<script>
  const socket = io();
  socket.on('NEW_NOTIFICATION', msg => console.log(msg));
</script>
